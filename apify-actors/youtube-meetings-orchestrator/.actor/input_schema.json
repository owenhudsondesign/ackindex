{
  "title": "YouTube Meetings Orchestrator Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "youtubeUrls": {
      "title": "YouTube URLs",
      "type": "array",
      "description": "Array of YouTube video, playlist, channel URLs, or /streams page URLs",
      "editor": "stringList",
      "default": [],
      "example": ["https://www.youtube.com/@townofnantucket/streams"]
    },
    "channelIds": {
      "title": "Channel IDs",
      "type": "array",
      "description": "Array of YouTube channel IDs to monitor",
      "editor": "stringList",
      "default": []
    },
    "youtubeApiKey": {
      "title": "YouTube API Key",
      "type": "string",
      "description": "YouTube Data API v3 key for video discovery and metadata",
      "editor": "textfield",
      "isSecret": true
    },
    "maxVideos": {
      "title": "Max Videos",
      "type": "integer",
      "description": "Maximum videos to process per run",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "openaiApiKey": {
      "title": "OpenAI API Key",
      "type": "string",
      "description": "OpenAI API key for GPT analysis and embeddings",
      "editor": "textfield",
      "isSecret": true
    },
    "openaiModel": {
      "title": "OpenAI Model",
      "type": "string",
      "description": "Model for meeting analysis",
      "enum": ["gpt-4o", "gpt-4o-mini", "gpt-4-turbo"],
      "default": "gpt-4o-mini",
      "editor": "select"
    },
    "enableEmbeddings": {
      "title": "Generate Embeddings",
      "type": "boolean",
      "description": "Generate semantic embeddings for search",
      "default": true
    },
    "actorIds": {
      "title": "Actor IDs (Advanced)",
      "type": "object",
      "description": "Custom actor IDs if you've forked/modified the actors",
      "editor": "json",
      "prefill": {
        "transcriptFetcher": "legible_radish/youtube-transcript-fetcher",
        "enricher": "legible_radish/meeting-ai-enrichment"
      }
    }
  },
  "required": ["youtubeUrls", "youtubeApiKey", "openaiApiKey"]
}
